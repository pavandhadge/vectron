// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.10.1
//   protoc               v3.19.6
// source: reranker/reranker.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  type CallOptions,
  type ChannelCredentials,
  Client,
  type ClientOptions,
  type ClientUnaryCall,
  type handleUnaryCall,
  makeGenericClientConstructor,
  type Metadata,
  type ServiceError,
  type UntypedServiceImplementation,
} from "@grpc/grpc-js";

export const protobufPackage = "vectron.reranker.v1";

/** Candidate represents a single search result that needs reranking. */
export interface Candidate {
  /** Vector ID */
  id: string;
  /** Original similarity score from vector search */
  score: number;
  /** Original vector (optional, for some strategies) */
  vector: number[];
  /** Associated metadata (e.g., title, content, tags) */
  metadata: { [key: string]: string };
}

export interface Candidate_MetadataEntry {
  key: string;
  value: string;
}

/** RerankRequest contains the query and candidates to rerank. */
export interface RerankRequest {
  /** Original user query */
  query: string;
  /** Candidates from vector search (top-K) */
  candidates: Candidate[];
  /** Number of results to return after reranking */
  topN: number;
  /** Optional strategy-specific parameters */
  options: { [key: string]: string };
}

export interface RerankRequest_OptionsEntry {
  key: string;
  value: string;
}

/** ScoredCandidate represents a reranked result with an enhanced score. */
export interface ScoredCandidate {
  /** Vector ID */
  id: string;
  /** New relevance score (0-1, higher is better) */
  rerankScore: number;
  /** Original similarity score */
  originalScore: number;
  /** Optional explanation of why this scored high/low */
  explanation: string;
}

/** RerankResponse contains the reordered results. */
export interface RerankResponse {
  /** Reranked and sorted results */
  results: ScoredCandidate[];
  /** Which strategy was applied (rule/llm/rl) */
  strategyUsed: string;
  /** Whether result came from cache */
  fromCache: boolean;
  /** Processing time in milliseconds */
  latencyMs: string;
}

/** GetStrategyRequest is empty for now. */
export interface GetStrategyRequest {
}

/** GetStrategyResponse returns info about the active strategy. */
export interface GetStrategyResponse {
  /** e.g., "rule-based", "llm", "rl" */
  strategyName: string;
  /** Strategy version */
  version: string;
  /** Strategy-specific config */
  config: { [key: string]: string };
}

export interface GetStrategyResponse_ConfigEntry {
  key: string;
  value: string;
}

/** InvalidateCacheRequest can target specific queries or clear all. */
export interface InvalidateCacheRequest {
  /** Specific query to invalidate (empty = all) */
  query: string;
  /** Specific candidates to invalidate */
  candidateIds: string[];
}

/** InvalidateCacheResponse confirms cache invalidation. */
export interface InvalidateCacheResponse {
  /** Number of cache entries removed */
  entriesCleared: number;
}

function createBaseCandidate(): Candidate {
  return { id: "", score: 0, vector: [], metadata: {} };
}

export const Candidate: MessageFns<Candidate> = {
  encode(message: Candidate, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.score !== 0) {
      writer.uint32(21).float(message.score);
    }
    writer.uint32(26).fork();
    for (const v of message.vector) {
      writer.float(v);
    }
    writer.join();
    globalThis.Object.entries(message.metadata).forEach(([key, value]: [string, string]) => {
      Candidate_MetadataEntry.encode({ key: key as any, value }, writer.uint32(34).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Candidate {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCandidate();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 21) {
            break;
          }

          message.score = reader.float();
          continue;
        }
        case 3: {
          if (tag === 29) {
            message.vector.push(reader.float());

            continue;
          }

          if (tag === 26) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.vector.push(reader.float());
            }

            continue;
          }

          break;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          const entry4 = Candidate_MetadataEntry.decode(reader, reader.uint32());
          if (entry4.value !== undefined) {
            message.metadata[entry4.key] = entry4.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Candidate {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      score: isSet(object.score) ? globalThis.Number(object.score) : 0,
      vector: globalThis.Array.isArray(object?.vector) ? object.vector.map((e: any) => globalThis.Number(e)) : [],
      metadata: isObject(object.metadata)
        ? (globalThis.Object.entries(object.metadata) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
    };
  },

  toJSON(message: Candidate): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.score !== 0) {
      obj.score = message.score;
    }
    if (message.vector?.length) {
      obj.vector = message.vector;
    }
    if (message.metadata) {
      const entries = globalThis.Object.entries(message.metadata) as [string, string][];
      if (entries.length > 0) {
        obj.metadata = {};
        entries.forEach(([k, v]) => {
          obj.metadata[k] = v;
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Candidate>, I>>(base?: I): Candidate {
    return Candidate.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Candidate>, I>>(object: I): Candidate {
    const message = createBaseCandidate();
    message.id = object.id ?? "";
    message.score = object.score ?? 0;
    message.vector = object.vector?.map((e) => e) || [];
    message.metadata = (globalThis.Object.entries(object.metadata ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseCandidate_MetadataEntry(): Candidate_MetadataEntry {
  return { key: "", value: "" };
}

export const Candidate_MetadataEntry: MessageFns<Candidate_MetadataEntry> = {
  encode(message: Candidate_MetadataEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Candidate_MetadataEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCandidate_MetadataEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Candidate_MetadataEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: Candidate_MetadataEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Candidate_MetadataEntry>, I>>(base?: I): Candidate_MetadataEntry {
    return Candidate_MetadataEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Candidate_MetadataEntry>, I>>(object: I): Candidate_MetadataEntry {
    const message = createBaseCandidate_MetadataEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseRerankRequest(): RerankRequest {
  return { query: "", candidates: [], topN: 0, options: {} };
}

export const RerankRequest: MessageFns<RerankRequest> = {
  encode(message: RerankRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.query !== "") {
      writer.uint32(10).string(message.query);
    }
    for (const v of message.candidates) {
      Candidate.encode(v!, writer.uint32(18).fork()).join();
    }
    if (message.topN !== 0) {
      writer.uint32(24).int32(message.topN);
    }
    globalThis.Object.entries(message.options).forEach(([key, value]: [string, string]) => {
      RerankRequest_OptionsEntry.encode({ key: key as any, value }, writer.uint32(34).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RerankRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRerankRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.query = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.candidates.push(Candidate.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.topN = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          const entry4 = RerankRequest_OptionsEntry.decode(reader, reader.uint32());
          if (entry4.value !== undefined) {
            message.options[entry4.key] = entry4.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RerankRequest {
    return {
      query: isSet(object.query) ? globalThis.String(object.query) : "",
      candidates: globalThis.Array.isArray(object?.candidates)
        ? object.candidates.map((e: any) => Candidate.fromJSON(e))
        : [],
      topN: isSet(object.topN) ? globalThis.Number(object.topN) : 0,
      options: isObject(object.options)
        ? (globalThis.Object.entries(object.options) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
    };
  },

  toJSON(message: RerankRequest): unknown {
    const obj: any = {};
    if (message.query !== "") {
      obj.query = message.query;
    }
    if (message.candidates?.length) {
      obj.candidates = message.candidates.map((e) => Candidate.toJSON(e));
    }
    if (message.topN !== 0) {
      obj.topN = Math.round(message.topN);
    }
    if (message.options) {
      const entries = globalThis.Object.entries(message.options) as [string, string][];
      if (entries.length > 0) {
        obj.options = {};
        entries.forEach(([k, v]) => {
          obj.options[k] = v;
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RerankRequest>, I>>(base?: I): RerankRequest {
    return RerankRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RerankRequest>, I>>(object: I): RerankRequest {
    const message = createBaseRerankRequest();
    message.query = object.query ?? "";
    message.candidates = object.candidates?.map((e) => Candidate.fromPartial(e)) || [];
    message.topN = object.topN ?? 0;
    message.options = (globalThis.Object.entries(object.options ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseRerankRequest_OptionsEntry(): RerankRequest_OptionsEntry {
  return { key: "", value: "" };
}

export const RerankRequest_OptionsEntry: MessageFns<RerankRequest_OptionsEntry> = {
  encode(message: RerankRequest_OptionsEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RerankRequest_OptionsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRerankRequest_OptionsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RerankRequest_OptionsEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: RerankRequest_OptionsEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RerankRequest_OptionsEntry>, I>>(base?: I): RerankRequest_OptionsEntry {
    return RerankRequest_OptionsEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RerankRequest_OptionsEntry>, I>>(object: I): RerankRequest_OptionsEntry {
    const message = createBaseRerankRequest_OptionsEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseScoredCandidate(): ScoredCandidate {
  return { id: "", rerankScore: 0, originalScore: 0, explanation: "" };
}

export const ScoredCandidate: MessageFns<ScoredCandidate> = {
  encode(message: ScoredCandidate, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.rerankScore !== 0) {
      writer.uint32(21).float(message.rerankScore);
    }
    if (message.originalScore !== 0) {
      writer.uint32(29).float(message.originalScore);
    }
    if (message.explanation !== "") {
      writer.uint32(34).string(message.explanation);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ScoredCandidate {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseScoredCandidate();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 21) {
            break;
          }

          message.rerankScore = reader.float();
          continue;
        }
        case 3: {
          if (tag !== 29) {
            break;
          }

          message.originalScore = reader.float();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.explanation = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ScoredCandidate {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      rerankScore: isSet(object.rerankScore) ? globalThis.Number(object.rerankScore) : 0,
      originalScore: isSet(object.originalScore) ? globalThis.Number(object.originalScore) : 0,
      explanation: isSet(object.explanation) ? globalThis.String(object.explanation) : "",
    };
  },

  toJSON(message: ScoredCandidate): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.rerankScore !== 0) {
      obj.rerankScore = message.rerankScore;
    }
    if (message.originalScore !== 0) {
      obj.originalScore = message.originalScore;
    }
    if (message.explanation !== "") {
      obj.explanation = message.explanation;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ScoredCandidate>, I>>(base?: I): ScoredCandidate {
    return ScoredCandidate.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ScoredCandidate>, I>>(object: I): ScoredCandidate {
    const message = createBaseScoredCandidate();
    message.id = object.id ?? "";
    message.rerankScore = object.rerankScore ?? 0;
    message.originalScore = object.originalScore ?? 0;
    message.explanation = object.explanation ?? "";
    return message;
  },
};

function createBaseRerankResponse(): RerankResponse {
  return { results: [], strategyUsed: "", fromCache: false, latencyMs: "0" };
}

export const RerankResponse: MessageFns<RerankResponse> = {
  encode(message: RerankResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.results) {
      ScoredCandidate.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.strategyUsed !== "") {
      writer.uint32(18).string(message.strategyUsed);
    }
    if (message.fromCache !== false) {
      writer.uint32(24).bool(message.fromCache);
    }
    if (message.latencyMs !== "0") {
      writer.uint32(32).int64(message.latencyMs);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RerankResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRerankResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.results.push(ScoredCandidate.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.strategyUsed = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.fromCache = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.latencyMs = reader.int64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RerankResponse {
    return {
      results: globalThis.Array.isArray(object?.results)
        ? object.results.map((e: any) => ScoredCandidate.fromJSON(e))
        : [],
      strategyUsed: isSet(object.strategyUsed) ? globalThis.String(object.strategyUsed) : "",
      fromCache: isSet(object.fromCache) ? globalThis.Boolean(object.fromCache) : false,
      latencyMs: isSet(object.latencyMs) ? globalThis.String(object.latencyMs) : "0",
    };
  },

  toJSON(message: RerankResponse): unknown {
    const obj: any = {};
    if (message.results?.length) {
      obj.results = message.results.map((e) => ScoredCandidate.toJSON(e));
    }
    if (message.strategyUsed !== "") {
      obj.strategyUsed = message.strategyUsed;
    }
    if (message.fromCache !== false) {
      obj.fromCache = message.fromCache;
    }
    if (message.latencyMs !== "0") {
      obj.latencyMs = message.latencyMs;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RerankResponse>, I>>(base?: I): RerankResponse {
    return RerankResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RerankResponse>, I>>(object: I): RerankResponse {
    const message = createBaseRerankResponse();
    message.results = object.results?.map((e) => ScoredCandidate.fromPartial(e)) || [];
    message.strategyUsed = object.strategyUsed ?? "";
    message.fromCache = object.fromCache ?? false;
    message.latencyMs = object.latencyMs ?? "0";
    return message;
  },
};

function createBaseGetStrategyRequest(): GetStrategyRequest {
  return {};
}

export const GetStrategyRequest: MessageFns<GetStrategyRequest> = {
  encode(_: GetStrategyRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetStrategyRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetStrategyRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): GetStrategyRequest {
    return {};
  },

  toJSON(_: GetStrategyRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<GetStrategyRequest>, I>>(base?: I): GetStrategyRequest {
    return GetStrategyRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetStrategyRequest>, I>>(_: I): GetStrategyRequest {
    const message = createBaseGetStrategyRequest();
    return message;
  },
};

function createBaseGetStrategyResponse(): GetStrategyResponse {
  return { strategyName: "", version: "", config: {} };
}

export const GetStrategyResponse: MessageFns<GetStrategyResponse> = {
  encode(message: GetStrategyResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.strategyName !== "") {
      writer.uint32(10).string(message.strategyName);
    }
    if (message.version !== "") {
      writer.uint32(18).string(message.version);
    }
    globalThis.Object.entries(message.config).forEach(([key, value]: [string, string]) => {
      GetStrategyResponse_ConfigEntry.encode({ key: key as any, value }, writer.uint32(26).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetStrategyResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetStrategyResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.strategyName = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.version = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          const entry3 = GetStrategyResponse_ConfigEntry.decode(reader, reader.uint32());
          if (entry3.value !== undefined) {
            message.config[entry3.key] = entry3.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetStrategyResponse {
    return {
      strategyName: isSet(object.strategyName) ? globalThis.String(object.strategyName) : "",
      version: isSet(object.version) ? globalThis.String(object.version) : "",
      config: isObject(object.config)
        ? (globalThis.Object.entries(object.config) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
    };
  },

  toJSON(message: GetStrategyResponse): unknown {
    const obj: any = {};
    if (message.strategyName !== "") {
      obj.strategyName = message.strategyName;
    }
    if (message.version !== "") {
      obj.version = message.version;
    }
    if (message.config) {
      const entries = globalThis.Object.entries(message.config) as [string, string][];
      if (entries.length > 0) {
        obj.config = {};
        entries.forEach(([k, v]) => {
          obj.config[k] = v;
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetStrategyResponse>, I>>(base?: I): GetStrategyResponse {
    return GetStrategyResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetStrategyResponse>, I>>(object: I): GetStrategyResponse {
    const message = createBaseGetStrategyResponse();
    message.strategyName = object.strategyName ?? "";
    message.version = object.version ?? "";
    message.config = (globalThis.Object.entries(object.config ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseGetStrategyResponse_ConfigEntry(): GetStrategyResponse_ConfigEntry {
  return { key: "", value: "" };
}

export const GetStrategyResponse_ConfigEntry: MessageFns<GetStrategyResponse_ConfigEntry> = {
  encode(message: GetStrategyResponse_ConfigEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetStrategyResponse_ConfigEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetStrategyResponse_ConfigEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetStrategyResponse_ConfigEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: GetStrategyResponse_ConfigEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetStrategyResponse_ConfigEntry>, I>>(base?: I): GetStrategyResponse_ConfigEntry {
    return GetStrategyResponse_ConfigEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetStrategyResponse_ConfigEntry>, I>>(
    object: I,
  ): GetStrategyResponse_ConfigEntry {
    const message = createBaseGetStrategyResponse_ConfigEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseInvalidateCacheRequest(): InvalidateCacheRequest {
  return { query: "", candidateIds: [] };
}

export const InvalidateCacheRequest: MessageFns<InvalidateCacheRequest> = {
  encode(message: InvalidateCacheRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.query !== "") {
      writer.uint32(10).string(message.query);
    }
    for (const v of message.candidateIds) {
      writer.uint32(18).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): InvalidateCacheRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseInvalidateCacheRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.query = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.candidateIds.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): InvalidateCacheRequest {
    return {
      query: isSet(object.query) ? globalThis.String(object.query) : "",
      candidateIds: globalThis.Array.isArray(object?.candidateIds)
        ? object.candidateIds.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: InvalidateCacheRequest): unknown {
    const obj: any = {};
    if (message.query !== "") {
      obj.query = message.query;
    }
    if (message.candidateIds?.length) {
      obj.candidateIds = message.candidateIds;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<InvalidateCacheRequest>, I>>(base?: I): InvalidateCacheRequest {
    return InvalidateCacheRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<InvalidateCacheRequest>, I>>(object: I): InvalidateCacheRequest {
    const message = createBaseInvalidateCacheRequest();
    message.query = object.query ?? "";
    message.candidateIds = object.candidateIds?.map((e) => e) || [];
    return message;
  },
};

function createBaseInvalidateCacheResponse(): InvalidateCacheResponse {
  return { entriesCleared: 0 };
}

export const InvalidateCacheResponse: MessageFns<InvalidateCacheResponse> = {
  encode(message: InvalidateCacheResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.entriesCleared !== 0) {
      writer.uint32(8).int32(message.entriesCleared);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): InvalidateCacheResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseInvalidateCacheResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.entriesCleared = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): InvalidateCacheResponse {
    return { entriesCleared: isSet(object.entriesCleared) ? globalThis.Number(object.entriesCleared) : 0 };
  },

  toJSON(message: InvalidateCacheResponse): unknown {
    const obj: any = {};
    if (message.entriesCleared !== 0) {
      obj.entriesCleared = Math.round(message.entriesCleared);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<InvalidateCacheResponse>, I>>(base?: I): InvalidateCacheResponse {
    return InvalidateCacheResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<InvalidateCacheResponse>, I>>(object: I): InvalidateCacheResponse {
    const message = createBaseInvalidateCacheResponse();
    message.entriesCleared = object.entriesCleared ?? 0;
    return message;
  },
};

/**
 * RerankService provides intelligent reranking of search results
 * to improve relevance and correctness beyond simple distance metrics.
 */
export type RerankServiceService = typeof RerankServiceService;
export const RerankServiceService = {
  /**
   * Rerank takes a query and a list of candidate results from vector search,
   * and returns a reordered list with enhanced relevance scores.
   */
  rerank: {
    path: "/vectron.reranker.v1.RerankService/Rerank",
    requestStream: false,
    responseStream: false,
    requestSerialize: (value: RerankRequest): Buffer => Buffer.from(RerankRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): RerankRequest => RerankRequest.decode(value),
    responseSerialize: (value: RerankResponse): Buffer => Buffer.from(RerankResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): RerankResponse => RerankResponse.decode(value),
  },
  /** GetStrategy returns the currently active reranking strategy. */
  getStrategy: {
    path: "/vectron.reranker.v1.RerankService/GetStrategy",
    requestStream: false,
    responseStream: false,
    requestSerialize: (value: GetStrategyRequest): Buffer => Buffer.from(GetStrategyRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): GetStrategyRequest => GetStrategyRequest.decode(value),
    responseSerialize: (value: GetStrategyResponse): Buffer => Buffer.from(GetStrategyResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): GetStrategyResponse => GetStrategyResponse.decode(value),
  },
  /** InvalidateCache clears cached reranking results (useful after feedback). */
  invalidateCache: {
    path: "/vectron.reranker.v1.RerankService/InvalidateCache",
    requestStream: false,
    responseStream: false,
    requestSerialize: (value: InvalidateCacheRequest): Buffer =>
      Buffer.from(InvalidateCacheRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): InvalidateCacheRequest => InvalidateCacheRequest.decode(value),
    responseSerialize: (value: InvalidateCacheResponse): Buffer =>
      Buffer.from(InvalidateCacheResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): InvalidateCacheResponse => InvalidateCacheResponse.decode(value),
  },
} as const;

export interface RerankServiceServer extends UntypedServiceImplementation {
  /**
   * Rerank takes a query and a list of candidate results from vector search,
   * and returns a reordered list with enhanced relevance scores.
   */
  rerank: handleUnaryCall<RerankRequest, RerankResponse>;
  /** GetStrategy returns the currently active reranking strategy. */
  getStrategy: handleUnaryCall<GetStrategyRequest, GetStrategyResponse>;
  /** InvalidateCache clears cached reranking results (useful after feedback). */
  invalidateCache: handleUnaryCall<InvalidateCacheRequest, InvalidateCacheResponse>;
}

export interface RerankServiceClient extends Client {
  /**
   * Rerank takes a query and a list of candidate results from vector search,
   * and returns a reordered list with enhanced relevance scores.
   */
  rerank(
    request: RerankRequest,
    callback: (error: ServiceError | null, response: RerankResponse) => void,
  ): ClientUnaryCall;
  rerank(
    request: RerankRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: RerankResponse) => void,
  ): ClientUnaryCall;
  rerank(
    request: RerankRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: RerankResponse) => void,
  ): ClientUnaryCall;
  /** GetStrategy returns the currently active reranking strategy. */
  getStrategy(
    request: GetStrategyRequest,
    callback: (error: ServiceError | null, response: GetStrategyResponse) => void,
  ): ClientUnaryCall;
  getStrategy(
    request: GetStrategyRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: GetStrategyResponse) => void,
  ): ClientUnaryCall;
  getStrategy(
    request: GetStrategyRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: GetStrategyResponse) => void,
  ): ClientUnaryCall;
  /** InvalidateCache clears cached reranking results (useful after feedback). */
  invalidateCache(
    request: InvalidateCacheRequest,
    callback: (error: ServiceError | null, response: InvalidateCacheResponse) => void,
  ): ClientUnaryCall;
  invalidateCache(
    request: InvalidateCacheRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: InvalidateCacheResponse) => void,
  ): ClientUnaryCall;
  invalidateCache(
    request: InvalidateCacheRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: InvalidateCacheResponse) => void,
  ): ClientUnaryCall;
}

export const RerankServiceClient = makeGenericClientConstructor(
  RerankServiceService,
  "vectron.reranker.v1.RerankService",
) as unknown as {
  new (address: string, credentials: ChannelCredentials, options?: Partial<ClientOptions>): RerankServiceClient;
  service: typeof RerankServiceService;
  serviceName: string;
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
